server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
              - "http://localhost"
            allowedMethods: [GET, POST, PUT, DELETE, OPTIONS]
            allowedHeaders: "*"
      routes:
        - id: iam
          uri: ${IAM_SERVICE_URL:http://localhost:8091}
          predicates:
            - Path=/api/iam/**
        - id: pet-catalog
          uri: ${PET_CATALOG_SERVICE_URL:http://localhost:8092}
          predicates:
            - Path=/api/pets/**
        - id: reports
          uri: ${REPORTS_SERVICE_URL:http://localhost:8093}
          predicates:
            - Path=/api/reports/**
        - id: geo
          uri: ${GEO_SERVICE_URL:http://localhost:8094}
          predicates:
            - Path=/api/zones/**
        - id: media
          uri: ${MEDIA_SERVICE_URL:http://localhost:8095}
          predicates:
            - Path=/api/media/**
        - id: matching
          uri: ${MATCHING_SERVICE_URL:http://localhost:8096}
          predicates:
            - Path=/api/matching/**
        - id: capacity
          uri: ${CAPACITY_SERVICE_URL:http://localhost:8097}
          predicates:
            - Path=/api/capacity/**
        - id: audit
          uri: ${AUDIT_SERVICE_URL:http://localhost:8098}
          predicates:
            - Path=/api/audit/**
        - id: bff
          uri: ${BFF_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/bff/**

security:
  jwt:
    secret: "sanos-y-salvos-super-secret-key-at-least-32-chars"
