# MySQL al que se conectan los microservicios desde Docker (Compose).
MYSQL_HOST=host.docker.internal
MYSQL_PORT=3306
MYSQL_USER=sanos
MYSQL_PASSWORD=sanos_pwd

# Opcional: mismo secreto en IAM y gateway (Compose ya lo pasa por defecto).
# SANOS_JWT_SECRET=tu-clave-de-al-menos-32-caracteres
